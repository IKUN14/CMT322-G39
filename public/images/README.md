# 背景图片说明

请将您的背景图片文件放在此目录下，命名为 `background.jpg`。

支持的图片格式：`.jpg`, `.jpeg`, `.png`, `.webp`

建议图片尺寸：1920x1080 或更大，以获得最佳显示效果。

如果图片文件名不同，请修改 `src/views/auth/LoginView.vue`、`src/views/auth/RegisterView.vue` 和 `src/views/auth/ForgotPasswordView.vue` 中的背景图片路径。

